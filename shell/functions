function gifit {
  ruby ~/.dotfiles/dev_scripts/screengif/screengif.rb --input $1 --output $2
}

function notes {
  code $DROPBOX/Notes/$1
}

function f {
  git checkout -b feature/$1
}

function fix {
  git checkout -b fix/$1
}
